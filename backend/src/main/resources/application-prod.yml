spring:
  jpa:
    hibernate:
      ddl-auto: update

  mail:
    properties:
      mail:
        smtp:
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

app:
  security:
    admin-cookie-secure: true
